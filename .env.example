# ===========================================
# SUPABASE (REQUERIDO)
# ===========================================
# Get these values from your Supabase project dashboard (Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# REQUIRED for production: Service Role Key (server-side operations, bypasses RLS)
# Used for cross-user notifications, moderation actions, etc.
# WARNING: Never expose this key to the client — server-side ONLY
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ===========================================
# SITE CONFIGURATION (REQUERIDO)
# ===========================================
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ===========================================
# ERROR TRACKING - SENTRY (Recomendado)
# ===========================================
# Get from https://sentry.io - Create Next.js project
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# ===========================================
# ANALYTICS - GOOGLE ANALYTICS (Opcional)
# ===========================================
# Get from https://analytics.google.com
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# ===========================================
# EMAILS - RESEND (Opcional)
# ===========================================
# Get from https://resend.com
RESEND_API_KEY=
EMAIL_FROM=TS Rating <noreply@tudominio.com>

# ===========================================
# CAPTCHA - CLOUDFLARE TURNSTILE (Recomendado para producción)
# ===========================================
# Free, privacy-friendly CAPTCHA for login/registration
# Get from https://dash.cloudflare.com → Turnstile → Add site
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# ===========================================
# RATE LIMITING - UPSTASH REDIS (Recomendado para producción)
# ===========================================
# Required for distributed rate limiting in production (serverless/multi-instance).
# Without these, rate limiting falls back to in-memory (single-instance only).
# Get from https://upstash.com — create a Redis database
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
